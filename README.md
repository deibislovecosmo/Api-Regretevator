# Api-Regretevator
Api bastante simple de regretevator,sientete libre de usarla
API creada con Express para mostrar personajes.

## Endpoints

GET /characters
GET /characters/:id
GET /characters/search?name=

## Instalación

npm install
npm start
